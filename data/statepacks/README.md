# StatePack Manifests

This directory stores portable manifests that describe parser, resolver and policy bindings per state.

- `kerala_statepack.yaml` is the reference implementation.
- Add one manifest per state when onboarding.

